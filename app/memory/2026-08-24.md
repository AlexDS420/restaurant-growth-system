# 2026-08-24 (noche) — Restaurant OS: cierre de entrega

## Avance del turno
- Auditoría global: columna real es `user_email` (no `actor_email`); confirmado `order.accepted → owner@casaaurora.pe/owner`. Los eventos de cliente anónimo quedan sin email (correcto) y el nombre del actor vive en `order_status_history.actor_name`.
- `account.html` verificado en navegador: login como Marta Ríos → "Sesión iniciada", perfil editable, enlaces a panel/tienda. El problema del header vacío era del snapshot, no de la app.
- **Bug real corregido en Analítica**: `a.by_payment.find(...)?.n * 0` siempre mostraba S/ 0.00. Fix: la query `by_payment` ahora suma `s` (monto por estado) y el panel muestra "n pedidos · monto". Verificado: `paid: n=2 s=7080`.
- **Bug corregido en seed**: `npm run seed` no hacía nada (seed.js sin bootstrap CLI). Añadido `isCli` con `openDb()` (aplica migraciones) + `seed(db)` + close. Ahora funciona con cualquier `DB_PATH`.
- **Scripts ops sin comandos de borrado** (Safety Guard denegó la v1 con `rm -f`): `rollback.sh` hace checkpoint WAL + `journal_mode=DELETE`, archiva `-wal/-shm` con `mv` a `backups/quarantine/`, preserva pre-rollback, restaura y verifica `integrity_check`. `rehearsal.sh` usa `data/rehearsal-<stamp>.db` (nunca borra). **Ensayo ejecutado: EXITOSO** (2 pedidos intactos tras rollback).
- Healthz real es `/api/v1/healthz` (no `/healthz`); corregidos README, runbook-despliegue, operaciones, informe-pruebas.
- Suite: `npm test` 21/21 tras los cambios. Storefront re-verificado en navegador (banner de error en es-PE, sin errores JS).
- Sync final: `restaurant-os/` → `~/Documents/Restaurant Groowth System/app/` (excluye data/*.db*, backups/, .env; scripts incluidos).

## Pendiente
- Verificador independiente (subagent `verificador-entrega`): integrar hallazgos antes del cierre.
- Respuesta final + update_goal complete (goal cc21771d / 29ee658d) solo con veredicto del verificador.

## Notas lecciones aprendidas
- `node --test tests/` falla en Node 24 (trailing slash) → usar `node --test`.
- El Safety Guard del entorno bloquea scripts con `rm -f`; implementar ops SIEMPRE sin comandos de borrado (mv a cuarentena / DBs con timestamp).
- Verificación en navegador: snapshots se truncan; usar `document.title` (ya no, se eliminó la instrumentación) y doble snapshot.
- storefront.html: dataset → string; usar `Number(id)` en lookups por id.